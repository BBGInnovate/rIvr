<div id="template-popup" class="template-popup">
<%= form_for template, :url=> templates_path, :html => {:method=>'post',:multipart => true, :id=>"frm-upload-logo"} do |f| %>
<div class="inputSpacing"> 
  <input type="hidden" name="todo" value="" />
  <%= f.hidden_field :name %>
  <%= f.hidden_field :id %>
  <%= f.hidden_field :branch_id %>
		<input type="hidden" name="branch_id" value="<%= template.branch_id %>" />
		<div class='error'>
    <%= flash[:error] %>
  </div>
  <div class='notice'>
    <%= flash[:notice] %>
  </div>
		<% if template.name =~ /introduction/ %>
    	<% if template.kind_of?(Vote) %>
		      <div class="title">Enter a name for this Vote/Poll</div>		
		      <input type="text" name="<%= template.class.name.downcase%>[identifier]" value="<%= template.identifier %>" />
								<!--
								<input type="text" list="cookies" name="<%#= template.class.name.downcase%>[identifier]" placeholder="Vote/Poll Name"/>
        <datalist id="cookies">
        	<%# template.branch.voting_sessions.each do | v| %>
         <option value="<%#=v.name%>" label="<%#= v.name %>"></option>
         <%# end %>
         </datalist>
									-->
					<% end %>
		<% elsif !template.find_introduction %>
		  <div class='error'>
       You have to upload Introdution Voice Prompt first
    </div>
		<% else %>
		  <% if (template.kind_of? Vote)
         voting_name = template.identifier %>
         <input type="hidden" name="<%= template.class.name.downcase%>[identifier]" value="<%= voting_name %>" />
    <% end %>
		<% end %>


		<p></p>
  <h2>Select your <%= template.name_map(template.name) %> file to upload</h2>
  <div class='fileBrowseWrap'>
    <%= f.file_field :sound %>
  </div>
  <div class="btnUploadWrap">
    <span><%= submit_tag 'Preview', :id=>"preview", :value=>"Upload (Preview)", :class=>"btnAction" %></span>
    <% if @preview || @save %>
      <span><%= submit_tag 'Save', :id=>"save", :class=>"btnAction" %></span>
    <% end %>
    <span><%= submit_tag 'Cancel', :id=>"cancel", :class=>"btnAction" %></span>
  </div>
  <div class="clear">
    <% if @preview %>
    <% src="/#{template.audio_link}" %>
    <object data="<%= src %>" type="application/x-mplayer2" width="300" height="60">
      <param name="filename" value="<%= src %>">
    </object><!--
    <embed type="application/x-mplayer2" src="<%= src %>" width="300" height="60"></embed>
    --><% end %>
  </div>
  
</div>
<% end %>
</div>

