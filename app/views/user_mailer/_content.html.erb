<h2 class="print-header"><%= @report_name %></h2>
<span class="print-date"><%= @start_date %> - <%= @end_date %> </span>
<table border=1 class="search-results-table">
  <thead>
    <tr>
    	<% @title.each do |f| %>
      <th>
        <%= f %>
      </th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @rows.each do | row | %>
    <tr>
      <% @title.each do |f| %>
      <td>
        <%= row[f] %>
      </td>
      <% end %>
    </tr>
    <% end %>
  </tbody>
</table>
<table border=1 class="search-results-table">
  <thead>
    <tr>
      <% @country_title.each do |f| %>
      <th>
        <%= f %>
      </th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @country_rows.each do | row | %>
    <tr>
      <% @country_title.each do |f| %>
      <td>
        <%= row[f] %>
      </td>
      <% end %>
    </tr>
    <% end %>
  </tbody>
</table>


